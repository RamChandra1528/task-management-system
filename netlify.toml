[build]
  command = "npm run build --workspace frontend"
  publish = "frontend/dist"
  functions = "backend"

[context.production]
  command = "npm run build --workspace frontend"
  publish = "frontend/dist"

[dev]
  command = "npm run dev"
  port = 5173
